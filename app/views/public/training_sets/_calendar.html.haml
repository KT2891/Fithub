= month_calendar events: @training_sets do |date, events|
  = date.day
  - events.each do |event|
    %div
      = link_to training_set_path(event) do
        = image_tag asset_path("dumbbell.png"), class: "ml-4 training_icon"