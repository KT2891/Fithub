%h3
  = t("requests")
%table.table
  %thead
    %tr
      %th ID
      %th タイトル
      %th 日時
      %th
      %th 閲覧
  = render partial: "admin/requests/show", collection: @requests, as: "request"
= render partial: "admin/requests/modal", collection: @requests, as: "request"